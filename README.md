# gRPC
demo on grpc setup

1) Show build.gradle and the respected outcomes
2) Show up the basic demo related to client server model.
   For a quick check whether the code is working.  
3) Should show the parser, where the data is parsing.
4) Cleanup things now and showup the newly created service
   that gets setup in proto.
5) Show once the service is created then 
   how can we use that to code practically.